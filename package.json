{
  "name": "@nostr-mail/core",
  "version": "0.1.0",
  "description": "NOSTR Mail protocol reference implementation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nostr-tools": "^2.10.0",
    "@cashu/cashu-ts": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
